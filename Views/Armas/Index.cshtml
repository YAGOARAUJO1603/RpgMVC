<!--Namespace da classe de Modelo para esta view-->
@model IEnumerable<RpgMvc.Models.ArmaViewModel>
<!--Inclua os TempData para Sucesso e Erro aqui, conforme exemplo na view de Autenticação-->

@if (@TempData["Mensagem"] != null)
{
    <div class="alert alert-success" role="alert">
        @Html.Raw(TempData["Mensagem"])</div>
}


@if (@TempData["MensagemErro"] != null)
{
    <div class="alert alert-danger" role="alert">
        @Html.Raw(TempData["MensagemErro"])</div>
}

@{
    ViewBag.Title = "Personagens"; 
}
<!--Título da página para o navegador-->
<h2>Relação de Armas</h2>
<!--Título da página-->
<p>
    <!--Links apontando para views na mesma pasta-->
    @Html.ActionLink("Criar Nova Arma", "Create")
</p>
<table class="table table-striped table-light">
    <tr class="table-primary">
        <!--Títulos das colunas da tabela-->
        <th>@Html.DisplayNameFor(model => model.Id)</th>
        <th>@Html.DisplayNameFor(model => model.Nome)</th>
        <th>@Html.DisplayNameFor(model => model.Dano)</th>
        <th>@Html.DisplayNameFor(model => model.Personagem)</th>
        <th>@Html.DisplayNameFor(model => model.PersonagemId)</th>
        <th></th>
    </tr>
    <!--Looping para escrever os dados na tabela-->
    @foreach (var item in Model)
    {
        <tr>
            <td>@Html.DisplayFor(modelItem => item.Id)</td>
            <td>@Html.DisplayFor(modelItem => item.Nome)</td>
            <td>@Html.DisplayFor(modelItem => item.Dano)</td>
            <td>@Html.DisplayFor(modelItem => item.Personagem)</td>
            <td>@Html.DisplayFor(modelItem => item.PersonagemId )</td>
            <td>
                <!--Coluna para Links/botões-->
            @Html.ActionLink("Editar", "Edit", new { id = item.Id } ) |
                @Html.ActionLink("Detalhes", "Details", new { id = item.Id }) |
                @Html.ActionLink("Deletar", "Delete", new { id = item.Id }
            , new {onclick = "return confirm('Deseja realmente deletar esta arma?');"}) | 
                @Html.ActionLink("Listar Armas", "Index", "Armas", new {id = item.Id})
            </td>
        
        </tr>
    }
</table>

@Html.ActionLink("Clique aqui para um embate com armas!!!", "Index", "Disputas", null, new { @class = "btn btn-warning"})
@Html.ActionLink("Zerar ranking e restaurar pontos vida", "ZerarRankingRestaurarVidas", "Personagens", null, new { @class = "btn btn-secondary", onclick = "return confirm('Deseja realmente realizar esta ação?;');"})